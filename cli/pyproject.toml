[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "codevault-cli"
version = "1.0.0"
description = "CodeVault CLI - Local compilation tool for license-protected executables"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = ["compiler", "nuitka", "license", "protection", "executable", "codevault"]
authors = [
    { name = "CodeVault Team" }
]
dependencies = [
    "requests==2.32.3",
    "typer[all]==0.12.5",
    "rich==13.9.4",
    "questionary==2.0.1",
    "pydantic==2.10.4",
    "pydantic-settings==2.7.1",
    "httpx==0.27.2",
    "anyio==4.8.0",
]

# Build tools (installed dynamically via npx, listed for reference):
# - pkg@5.8.1 (Node.js packaging)
# - javascript-obfuscator@4.1.0 (JS obfuscation)
# - nuitka>=2.0 (Python compilation, installed separately)
# See cli/compiler_constants.py for version pins
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Build Tools",
]

[project.scripts]
codevault-cli = "codevault_cli.app:run"
codevault = "codevault_cli.app:run"

[project.urls]
Homepage = "https://github.com/your-org/codevault"
Documentation = "https://github.com/your-org/codevault#readme"
Issues = "https://github.com/your-org/codevault/issues"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.build.targets.sdist]
include = [
    "*.py",
    "README.md",
]
