# License-Wrapper Dependencies

# CLI Tool
click>=8.1.0
rich>=13.0.0
keyring>=24.0.0  # Secure credential storage (Windows Credential Manager, macOS Keychain, Linux Secret Service)

# Email Services
sendgrid>=6.11.0  # SendGrid email service (optional)
resend>=2.0.0  # Resend email service (optional)

# License Core (injected into wrapped apps)
requests>=2.28.0
cryptography>=40.0.0

# Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic[email]>=2.5.0
python-multipart>=0.0.6
bcrypt>=4.1.0
PyJWT>=2.8.0
python-dotenv>=1.0.0
httpx>=0.25.0
stripe>=7.0.0
geoip2>=4.8.0
boto3>=1.34.0
urllib3>=2.2.2

# Database (PostgreSQL)
asyncpg>=0.29.0

# Caching (required for rate limiting)
redis>=5.0.0

# Compilation (required for Nuitka compilation)
nuitka>=2.0
ordered-set>=4.0.0
zstandard>=0.15.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
